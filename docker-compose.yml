services:
  redis:
    image: redis:7-alpine
    container_name: dispatchai-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redisdata:/data

volumes:
  redisdata: